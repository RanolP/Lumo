=== product binds tighter than sum ===
:parse(Lumo)

fn t() = 1 + 2 * 3

---

(FILE (ITEM (FN_DECL (PARAM_LIST) (EXPR_BODY (EXPR_INFIX (NUMBER_EXPR) (EXPR_INFIX (NUMBER_EXPR) (NUMBER_EXPR)))))))

=== sum is left-associative ===
:parse(Lumo)

fn t() = 1 - 2 - 3

---

(FILE (ITEM (FN_DECL (PARAM_LIST) (EXPR_BODY (EXPR_INFIX (EXPR_INFIX (NUMBER_EXPR) (NUMBER_EXPR)) (NUMBER_EXPR))))))

=== power is right-associative ===
:parse(Lumo)

fn t() = 2 ** 3 ** 4

---

(FILE (ITEM (FN_DECL (PARAM_LIST) (EXPR_BODY (EXPR_INFIX (NUMBER_EXPR) (EXPR_INFIX (NUMBER_EXPR) (NUMBER_EXPR)))))))

=== unary minus and bang ===
:parse(Lumo)

fn t(x) = -x * !x

---

(FILE (ITEM (FN_DECL (PARAM_LIST (PARAM)) (EXPR_BODY (EXPR_INFIX (EXPR_PREFIX (IDENT_EXPR)) (EXPR_PREFIX (IDENT_EXPR)))))))

=== parens override precedence ===
:parse(Lumo)

fn t() = (1 + 2) * 3

---

(FILE (ITEM (FN_DECL (PARAM_LIST) (EXPR_BODY (EXPR_INFIX (PAREN_EXPR (EXPR_INFIX (NUMBER_EXPR) (NUMBER_EXPR))) (NUMBER_EXPR))))))

=== block with let statement and string ===
:parse(Lumo)

fn t() = { let s = "hi\n"; s }

---

(FILE (ITEM (FN_DECL (PARAM_LIST) (EXPR_BODY (BLOCK_EXPR (LET_STMT (STRING_EXPR)) (EXPR_STMT (IDENT_EXPR)))))))

