# Built-in tactics: start minimal, expand later

The engine ships with a **minimal set of internal (built-in) tactics** and
expands it later as needs prove themselves. Built-ins so far: `subst`
(`$e[$b := $a]`, elab) and `hash` (see rows).
