# Chapter order and document policy

Chapters: project layout → syntax → elaboration → type; architecture
pillars after them. Documents are snapshots — no changelog, no rev
numbers, no dates, no legacy exposition. Chapters not yet designed stay
as one-line placeholders.
