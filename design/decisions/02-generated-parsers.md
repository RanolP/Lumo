# Parsers are generated

Parsers are generated from the `.syn.langue` files, with `praat` blocks for
expressions and extern recovery hooks. Fall back to a hand-written parser
only if recovery quality proves insufficient at M0 exit.
