// Ported from legacy/packages/libstd/src#js/io.lumo (D-45). Binding
// (console.log) lives in runtime/js/prelude.js; `resume` dropped.

extern fn __println(msg: String);

impl IO {
  fn println(msg: String) = __println(msg)
}
