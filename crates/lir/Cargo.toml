[package]
name = "lumo-lir"
version = "0.1.0"
edition = "2021"

[lib]
name = "lumo_lir"
path = "src/lib.rs"

[dependencies]
lumo-span = { path = "../span" }
lumo-types = { path = "../types" }
lumo-hir = { path = "../hir" }
