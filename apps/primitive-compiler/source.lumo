fn unit() {}

enum Nat {
  zero,
  succ(Nat),
}

enum List {
  nil,
  cons(Nat, List),
}


Nat.zero
Nat.succ
Nat.succ(Nat.zero)

List.nil
List.cons(Nat.zero, List.nil)

Nat.succ(Nat.succ(Nat.zero))
Nat.succ(unit())
Nat.succ(List.cons(Nat.zero, List.nil))
