export const title = "RFC 0016 — Contexts";

# RFC 0016 — Contexts

A **context** is a named multimap — theoretically a set of tuples:

```
context Γ = [Ident: TypeV]
```

Accessed in judgment rules as `Γ.$name`.
