export const title = "RFC 0018 — Losslessness is defined language by language";

# RFC 0018 — Losslessness is defined language by language

Whether a language preserves trivia (whitespace, comments) is decided by
that language's own definition — not a global property of the pipeline.
