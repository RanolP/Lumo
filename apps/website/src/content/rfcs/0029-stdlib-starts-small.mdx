export const title = "RFC 0029 — stdlib starts small, extends later";

# RFC 0029 — stdlib starts small, extends later

The Langue stdlib begins with a minimal surface (helper combinators such
as `foldr`/`map`, common token classes, builtin value shapes such as
`List`) and grows only as needs prove themselves — same philosophy as the
built-in tactics.
